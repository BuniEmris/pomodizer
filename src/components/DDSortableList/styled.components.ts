import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  itemStyle: {
    position: 'absolute',
    left: 0,
    top: 0,
    right: 0,
  },
  footerStyle: {
    position: 'absolute',
    left: 0,
    bottom: 0,
    right: 0,
  },
})
