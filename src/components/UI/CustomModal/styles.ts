import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  alertContainer: {
    borderRadius: 10,
    alignSelf: 'center',
    backgroundColor: 'white',
    padding: 30,
  },
})
